name: My first pipeline

on: push

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Check out code
              uses: actions/checkout@v2
            - name: build image
              run: docker build -t vniyigen/ciportifolio . 
            - name: List images
              run: docker images

    
            